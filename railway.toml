# Backend service (default configuration for root)
[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
watchPaths = ["backend/**"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8000"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
